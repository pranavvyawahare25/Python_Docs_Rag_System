# Python_Docs_Rag_System
The system ingests Python documentation, chunks it semantically while preserving structure, generates embeddings, and builds a FAISS vector index for efficient similarity search.
